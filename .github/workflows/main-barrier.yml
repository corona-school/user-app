name: User-App Main-Barrier

on:
  push:
  pull_request:
    branches: [ main ]
jobs:
 
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Install Dependencies
        run: npm ci
      - name: Build App
        run: npm run build