import { Canvas, Meta, Story } from '@storybook/addon-docs';
import AsNavigationItem from './AsNavigationItem';

<Meta title="Misc/AsNavigationItem" component={AsNavigationItem} />

# AsNavigationItem

-   this is just a wrapper component
-   it allows to control the active navigation path by using a effect to set `rootPath`on initial rendering

<Story name="As Navigation Item">
    <AsNavigationItem />
</Story>
