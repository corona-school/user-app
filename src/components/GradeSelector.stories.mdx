import { Canvas, Meta, Story } from '@storybook/addon-docs';
import { Row } from 'native-base';
import { GradeSelector } from './GradeSelector';
import { useState } from 'react';

<Meta title="Basic/GradeSelector" component={GradeSelector} />

# GradeSelector

Allows selection of grades. Some grades can optionally be hidden with the `omitGrades` prop

<Story name="GradeSelector">
    {() => {
        const [grade, setGrade] = useState(1);
        return (
            <Row flexWrap="wrap" width="100%">
                <GradeSelector grade={grade} onGradeChange={setGrade} />
            </Row>
        );
    }}
</Story>
