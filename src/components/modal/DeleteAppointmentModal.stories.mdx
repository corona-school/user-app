import { Canvas, Meta, Story } from '@storybook/addon-docs';
import DeleteAppointmentModal from './DeleteAppointmentModal';

<Meta title="Appointment/DeleteAppointmentModal" component={DeleteAppointmentModal} />

# DeleteAppointmentModal

<Story name="DeleteAppointmentModal">
    <DeleteAppointmentModal onDelete={() => console.log('delete appointment')} close={() => console.log('close modal because canceled')} />
</Story>
