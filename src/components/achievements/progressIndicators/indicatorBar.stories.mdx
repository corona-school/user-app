import { Canvas, Meta, Story } from '@storybook/addon-docs';
import IndicatorBar from './indicatorBar';

<Meta title="Achievements/ProgressIndicators/IndicatorBar" component={IndicatorBar} />

# IndicatorBar

-   The Indicator Bar is used to display the current progress of achievements.

<Story name="Indicator Bar">
    <IndicatorBar maxSteps={5} currentStep={2} />
</Story>
