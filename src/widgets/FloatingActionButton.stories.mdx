import { Canvas, Meta, Story } from '@storybook/addon-docs';
import FloatingActionButton from './FloatingActionButton';

<Meta title="Misc/FloatingActionButton" component={FloatingActionButton} />

# Floating Button

<Story name="FloatingActionButton">
    <FloatingActionButton handlePress={() => console.log('Add new appointment')} place={'top-left'} />
</Story>
