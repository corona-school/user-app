import { Canvas, Meta, Story } from '@storybook/addon-docs';
import FullPageModal from './FullPageModal';

<Meta title="FullPageModal" component={FullPageModal} />

# FullPageModal

-   not shown here because view state comes from `React.Context`

-   use `setShow, setContent and setVariant` to control modal

<Story name="FullPageModal">
    <FullPageModal />
</Story>
