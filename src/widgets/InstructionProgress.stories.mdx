import { Canvas, Meta, Story } from '@storybook/addon-docs'
import { NativeBaseProvider, Heading } from 'native-base'
import Theme from '../Theme';
import InstructionProgress from './InstructionProgress'

<Meta title='InstructionProgress' component={InstructionProgress} />

# InstructionProgress

* used in onboarding and data creation processes to show progress

<NativeBaseProvider theme={Theme}>
<InstructionProgress currentIndex={0} instructions={[
  {label: "My Label", content: [
    <Heading>Lorem</Heading>
  ], },
  {label: "My Label", content: [<Heading>Lorem</Heading>], },
  ]}/>
</NativeBaseProvider>