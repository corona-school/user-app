import { Canvas, Meta, Story } from '@storybook/addon-docs';
import { Heading, Button } from 'native-base';
import { TutorScreeningCard } from './TutorScreeningCard';

<Meta title="Screening/TutorScreeningCard" component={TutorScreeningCard} />

# TutorScreeningCard

The TutorScreeningCard shows information about a Tutor Screening:

Successful Screening:

<Story name="TutorScreeningCard / success">
    <TutorScreeningCard screening={{ createdAt: new Date(), success: true, screener: { firstname: 'Max', lastname: 'Mustermann' } }} />
</Story>

Rejected Screening:

<Story name="TutorScreeningCard / rejection">
    <TutorScreeningCard screening={{ createdAt: new Date(), success: false, screener: { firstname: 'Max', lastname: 'Mustermann' } }} />
</Story>
