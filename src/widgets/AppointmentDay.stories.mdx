import { Meta, Story } from '@storybook/addon-docs';
import AppointmentDay from './AppointmentDay';

<Meta title="Appointment/AppointmentDay" component={AppointmentDay} />

# Appointment Day

shows wheter a appointment is currently taking place (dark green) or not

<Story name="AppointmentDay">
    <AppointmentDay
        start="2023-01-03T16:00:00.547Z"
        duration={15}
        title="Code"
        organizers={['Andreas Müller']}
        participants={['Max']}
        appointmentId={2}
        appointmentType="match"
        position="10"
        total="15"
        displayName="React for Beginners"
        canJoinVideochat={true}
    />
    <AppointmentDay
        start={new Date().toISOString()}
        duration={15}
        title="Design"
        organizers={['Andreas Müller', 'Claudia Max']}
        participants={['Max', 'Tim', 'Tom', 'Nina', 'Emma']}
        appointmentId={1}
        appointmentType="group"
        position="1"
        total="5"
        isOrganizer={true}
        displayName="UX Design"
        canJoinVideochat={true}
    />
</Story>
