import { Canvas, Meta, Story } from '@storybook/addon-docs';
import { VStack, Button } from 'native-base';
import AlertMessage from './AlertMessage';
import AppointmentCard from './AppointmentCard';
import { DateTime } from 'luxon';

<Meta title="Dashboard/AppointmentCard" component={AppointmentCard} />

# AppointmentCard

-   use `isTeaser={true}` to show the appointment in highlighted state
-   pass dates as iso string
-   use `isGrid` to make card fill parent
-   `videoButton` is only shown in `isTeaser` cards

(adding `isTeaser` makes the component unrenderable in storybook?)

<Story name="AppointmentCard soon">
    <VStack minW="420px" maxW="33%">
        <AppointmentCard
            isGrid
            onPressToCourse={() => null}
            description={'My description'}
            tags={[{ name: 'Mathe' }, { name: 'Englisch' }]}
            date={new Date(Date.now() + 1000 * 60 * 90).toISOString()}
            duration={60}
            image={'https://picsum.photos/480/360'}
            title={'Lorem Ipsum'}
            videoButton={<Button>Hello World</Button>}
        />
    </VStack>
</Story>

<Story name="AppointmentCard just started">
    <VStack minW="420px" maxW="33%">
        <AppointmentCard
            isGrid
            onPressToCourse={() => null}
            description={'My description'}
            tags={[{ name: 'Mathe' }, { name: 'Englisch' }]}
            date={new Date().toISOString()}
            duration={60}
            image={'https://picsum.photos/480/360'}
            title={'Lorem Ipsum'}
            videoButton={<Button>Hello World</Button>}
        />
    </VStack>
</Story>

<Story name="AppointmentCard ended">
    <VStack minW="420px" maxW="33%">
        <AppointmentCard
            isGrid
            onPressToCourse={() => null}
            description={'My description'}
            tags={[{ name: 'Mathe' }, { name: 'Englisch' }]}
            date={new Date(Date.now() - 1000 * 60 * 61).toISOString()}
            duration={60}
            image={'https://picsum.photos/480/360'}
            title={'Lorem Ipsum'}
            videoButton={<Button>Hello World</Button>}
        />
    </VStack>
</Story>

