import { Meta, Story } from '@storybook/addon-docs';
import AppointmentCard from './AppointmentCard';

<Meta title="AppointmentCard" component={AppointmentCard} />

# AppointmentCard

This is how an empty appointment card looks like:

<Story name="Empty Appointment Card">
    <AppointmentCard />
</Story>
