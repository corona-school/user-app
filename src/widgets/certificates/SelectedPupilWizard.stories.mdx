import { Canvas, Meta, Story } from '@storybook/addon-docs';
import SelectedPupilWizard from './SelectedPupilWizard';

<Meta title="Misc/SelectedPupilWizard" component={SelectedPupilWizard} />

# SelectedPupilWizard

-   shows each pupil in certificate request
-   passes all pupils as `pupilCount: number`
-   `currentIndex` to page pupils
-   pass `match: LFMatch` to show data of users^

<Story name="SelectedPupilWizard">
    <SelectedPupilWizard
        pupilCount={12}
        currentIndex={0}
        match={{
            pupil: {
                firstname: 'Max',
                lastname: 'Mustermann',
            },
        }}
    />
</Story>
