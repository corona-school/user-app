import { Canvas, Meta, Story } from '@storybook/addon-docs'
import { NativeBaseProvider } from 'native-base'
import Theme from '../../Theme';
import SelectActionsWidget from './SelectActionsWidget'

<Meta title='SelectActionsWidget' component={SelectActionsWidget} />

# SelectActionsWidget

* select actions for certificate generation
* can pass "Sonstige" input to context via `setState`

<NativeBaseProvider theme={Theme}>
<SelectActionsWidget />
</NativeBaseProvider>