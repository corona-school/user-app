import { Canvas, Meta, Story } from '@storybook/addon-docs';
import PupilPager from './PupilPager';

<Meta title="PupilPager" component={PupilPager} />

# PupilPager

-   A widget to show pupils of the student for certificate selection
-   controlled via `React.Context` `pupilIndex` as paging

<Story name="PupilPager">
    <PupilPager />
</Story>
