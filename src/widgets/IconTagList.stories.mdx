import { Canvas, Meta, Story } from '@storybook/addon-docs';
import IconTagList from './IconTagList';

<Meta title="Basic/IconTagList" component={IconTagList} />

# IconTagList

-   use `initial` like a input `value` prop
-   iconPath starts at `./src/assets`
-   `textIcon` sets a string as icon

<Story name="IconTagList">
    <IconTagList text="Lorem" textIcon="3" />
</Story>
