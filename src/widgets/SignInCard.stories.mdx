import { Canvas, Meta, Story } from '@storybook/addon-docs'
import { NativeBaseProvider } from 'native-base'
import Theme from '../Theme';
import SignInCard from './SignInCard'

<Meta title='SignInCard' component={SignInCard} />

# SignInCard

* was used to show card to sign in to courses
* barely to not used right now


 * use `showTrafficLight` to indicate course participation
 * use `TrafficLightStatus` to differentiate between participation states
* `tags` use `LFTag` type
* `data`is of type `LFSubcourse`

<NativeBaseProvider theme={Theme}>
<SignInCard 
showTrafficLight
trafficLightStatus={"full"}
tags={[{name: "Englisch"}, {name: "Deutsch"}]} data={{
  course: {name: "Test Kurs", outline: "Lorem Ipsum", description: "My description"},
  lectures: [
    {start: new Date().toISOString(), duration: 60}
  ],
  
}}/>
</NativeBaseProvider>
