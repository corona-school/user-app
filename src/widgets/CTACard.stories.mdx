import { Canvas, Meta, Story } from '@storybook/addon-docs';
import { Text, Button } from 'native-base';
import CTACard from './CTACard';

<Meta title="CTACard" component={CTACard} />

# CTACard

-   `closable`is not used right now
-   with `content` and `button` dynamic content can be put into the card\_\_item
-   add an icon via `icon`
-   3 variants: `'normal' | 'outline' | 'dark'`

<Story name="CTACard">
    <CTACard title={'My title'} content={<Text>Content</Text>} button={<Button>My Button</Button>} />
</Story>
