import { Canvas, Meta, Story } from '@storybook/addon-docs';
import UserProgress from './UserProgress';

<Meta title="Basic/UserProgress" component={UserProgress} />

# UserProgress

-   Used to display a progress
-   Mainly used to display profile completeness
-   pass percentage as number/integer

<Story name="UserProgress">
    <UserProgress percent={0} showPercent />
    <UserProgress percent={50} showPercent />
    <UserProgress percent={100} showPercent />
</Story>
